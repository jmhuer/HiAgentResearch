# HiAgentControl pattern 3 — fill plan.json

```
/ulw-loop "Follow skill hac-assess-plan end-to-end. Enrich pre-seeded state/current/plan.json with exactly N evidence-backed tasks. Deliverable: state/current/plan.json only."
```

Replace `N` with task count (e.g. 5). All lint, gate, and exit rules live in the skill — not in this command.
