# Merge policy (Phase 2 stub)

`lineage.mode: force` is reserved for explicit promotion of a chosen experiment commit into another branch or `main`.

Phase 1 does not implement force-merge, promotion gates, or score-model merge controllers. Selecting `force` fails fast at bootstrap time.
